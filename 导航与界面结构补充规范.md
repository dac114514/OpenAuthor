# 导航与界面结构补充规范

> 本文档作为《规范》的独立补充，不修改原文档内容。  
> 本补充专门规定 App 的一级导航、项目级导航与 Material 3 Expressive 视觉一致性。

---

## 1. 总体导航原则

软件采用清晰的两级导航结构：

```text
App 主界面
├── 工作台
└── 设置

进入具体项目后
├── 总览
├── 设定
├── 大纲
├── 章节
└── AI
```

两级导航必须明确区分：

- “工作台”和“设置”属于整个 App。
- 五个项目 Tab 只属于当前打开的小说项目。
- 未进入项目时，不显示项目级 Tab。
- 退出项目后，返回 App 工作台。
- 不得把 App 级页面与项目级页面混在同一组底部导航中。

---

## 2. App 主界面

App 主界面只保留两个一级入口：

```text
工作台
设置
```

### 2.1 工作台

工作台是 App 默认首页，用于集中管理所有小说项目。

主要功能：

- 新建项目
- 打开项目
- 删除项目
- 导入项目
- 导出项目
- 显示最近编辑项目
- 显示项目生成状态
- 显示当前创作进度

项目卡片建议展示：

- 封面
- 书名
- 作者
- 当前阶段
- 当前连续完成章节
- 目标章节数
- 当前模型
- 最近更新时间
- 当前生成状态
- 一致性状态

点击项目卡片后，进入该项目的独立工作界面。

### 2.2 设置

设置页只管理 App 级配置，不展示具体项目内容。

主要内容：

- 模型配置管理
- API Key 管理
- 默认生成参数
- 默认章节字数
- 默认上下文范围
- 默认 Agent 执行模式
- 外观与主题
- 本地数据备份
- 导入导出设置
- 日志与诊断
- 关于
- 开源许可证

项目专属内容不得放入设置页。

---

## 3. 项目界面

进入具体项目后，显示五个项目级 Tab：

```text
总览
设定
大纲
章节
AI
```

这五个 Tab 只服务于当前项目。

### 3.1 总览

总览页用于快速查看项目整体状态。

建议展示：

- 项目基础信息
- 当前创作阶段
- 当前连续完成章节
- 目标章节数
- 世界观条目数量
- 人物数量
- 分卷数量
- 已创建细纲数量
- 已生成章节数量
- 一致性池状态
- 最近一次 Agent 运行
- 最近修改内容
- 快捷继续创作入口

总览页应以状态和入口为主，不承担复杂编辑任务。

### 3.2 设定

设定页集中管理所有长期创作资产：

- 世界观
- 人物
- 人物关系
- 人物弧光
- 文风
- 故事骨架
- 一致性池
- 活跃线索
- 伏笔
- 世界状态

各子页面应共享统一的卡片、列表和详情布局。

### 3.3 大纲

大纲页集中管理：

- 分卷
- 分卷大纲
- 章节细纲
- 章节类型
- 章节范围
- 细纲完成状态

分卷只能由用户手动创建。

AI 可以填写和修改分卷内容，但不能：

- 创建分卷
- 修改分卷章节范围
- 绕过分卷限制

### 3.4 章节

章节页集中管理：

- 章节列表
- 章节细纲状态
- 正文生成
- 连续章节
- 跳章章节
- 草稿
- 纯文本编辑
- AI 修改
- 接受章节
- 一致性更新
- 章节错误状态

章节页应以阅读、审阅和生成控制为主，而不是传统富文本码字界面。

### 3.5 AI

AI 页是当前项目的自由聊天与 Agent 控制中心。

主要内容：

- Manual 模式
- Solo 模式
- 当前作用域
- 当前可用工具
- 待确认操作
- Agent 运行状态
- 工具调用记录
- 失败与重试
- 项目级自由修改
- 当前上下文摘要

AI 页中的工具权限必须根据当前作用域动态限制。

---

## 4. 返回与跳转逻辑

导航行为必须保持稳定、可预测。

### 4.1 从 App 主界面进入项目

```text
工作台
→ 点击项目
→ 进入项目总览
```

### 4.2 从项目返回

项目界面的系统返回操作：

```text
项目界面
→ 返回工作台
```

不应直接退出 App。

### 4.3 项目内部切换

五个项目 Tab 之间切换时：

- 保留各页面滚动位置
- 保留当前筛选状态
- 保留未发送的聊天输入
- 不重复加载全部项目数据
- 不重建 Agent 会话

### 4.4 设置页返回

设置页返回时：

```text
设置
→ 工作台
```

设置页不进入具体项目导航层。

---

## 5. Material 3 Expressive 视觉规范

整个软件统一采用 Material 3 Expressive。

### 5.1 一致性要求

App 主界面与项目界面必须共享同一套：

- 色彩系统
- 字体层级
- 圆角体系
- 间距体系
- 图标体系
- 卡片样式
- 按钮样式
- 动效节奏
- 状态表达方式
- 错误提示方式

一级导航和项目级导航可以在结构上区分，但不能形成两套割裂的视觉风格。

### 5.2 禁止事项

禁止：

- 玻璃拟态
- 桌面后台式三栏布局
- Author 原界面照搬
- 密集工具栏
- 传统网页管理后台风格
- 不统一的圆角
- 不统一的动画
- 过多小图标
- 富文本编辑器式工具条
- 在同一页面堆叠过多操作入口

### 5.3 页面层级

推荐层级：

```text
一级页面
→ 功能分组
→ 内容卡片
→ 详情页
→ Bottom Sheet / Dialog
```

复杂参数和低频操作优先放入：

- Modal Bottom Sheet
- Dialog
- 二级详情页

不要让主页面承担所有操作。

### 5.4 动效

推荐使用：

- 容器形变
- 平滑页面切换
- 卡片展开与收起
- Agent 执行状态动画
- 生成中动态进度容器
- Manual 确认卡出现与消失
- Solo 自动执行过程反馈

动效必须服务于状态表达，不得为装饰而滥用。

---

## 6. 项目 Tab 的视觉处理

项目级五个 Tab 应明确属于当前项目。

推荐表现：

- 顶部显示项目名称或封面缩略图
- Tab 区域使用项目级容器包裹
- 当前 Tab 状态明显
- 切换动画统一
- 不与 App 主界面的一级导航混用同一视觉层级

建议结构：

```text
项目标题栏
项目 Tab 导航
当前页面内容
```

若使用底部项目导航，应确保：

- 离开项目后立即消失
- 与 App 主导航不会同时出现
- 返回工作台时恢复 App 主导航

---

## 7. 工作台与项目界面的关系

工作台负责“管理项目”。

项目界面负责“创作项目”。

必须避免以下混淆：

- 工作台直接编辑人物
- 设置页直接修改章节
- 项目总览显示所有 App 配置
- AI 页管理全局 API Key
- 项目内显示其它项目的数据

数据边界必须明确：

```text
App 级数据
├── 模型配置
├── API Key
├── 默认偏好
└── 全局设置

项目级数据
├── 世界观
├── 人物
├── 故事骨架
├── 分卷
├── 细纲
├── 章节
├── 一致性池
└── 项目聊天
```

---

## 8. Codex 实现约束

Codex 在实现导航时必须遵守：

1. App 主界面只能包含“工作台”和“设置”两个一级入口。
2. 五个项目 Tab 只能在打开项目后出现。
3. 不得把七个入口全部放入同一底部导航。
4. 项目返回操作必须回到工作台。
5. App 级设置与项目级设置必须分离。
6. 所有页面统一使用 Material 3 Expressive。
7. 不得引入玻璃拟态或 Author 原视觉。
8. Tab 切换必须保留页面状态。
9. 项目 Tab 不得重复创建 Agent 会话。
10. 页面结构必须优先适配手机竖屏。
11. 不增加平板、多语言或桌面布局。
12. 不改变原开发规范中的 Agent、存储与一致性规则。

---

## 9. 最终导航定义

最终导航结构固定为：

```text
App
├── 工作台
│   ├── 项目列表
│   ├── 新建项目
│   └── 导入导出
│
├── 设置
│   ├── 模型
│   ├── API Key
│   ├── 默认参数
│   ├── 数据
│   └── 关于
│
└── 当前项目
    ├── 总览
    ├── 设定
    ├── 大纲
    ├── 章节
    └── AI
```
设置界面仅为列表布局，具体设置项在二级界面

该结构为固定产品约束，后续开发不得随意改为单层导航。
